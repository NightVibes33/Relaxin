name: Box Recon

on:
  push:
    branches:
      - box-recon-20260821
    paths-ignore:
      - box-recon-results/**
  workflow_dispatch:

permissions:
  contents: write

jobs:
  recon:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: box-recon-20260821

      - name: Public API and CLI recon
        shell: bash
        run: |
          set -u
          mkdir -p box-recon-results
          exec > >(tee box-recon-results/recon.txt) 2>&1

          echo '=== context ==='
          date -u
          uname -a

          echo '=== docs/openapi ==='
          curl -fsSL --max-time 20 https://docs.ascii.dev/openapi/box-v1.yaml -o box-recon-results/box-v1.yaml || true
          if [ -s box-recon-results/box-v1.yaml ]; then
            echo "openapi_bytes=$(wc -c < box-recon-results/box-v1.yaml)"
            grep -nE '^  /|operationId:|security:|bearer|noEnv|safeForThirdParties|fork|snapshot|desktop|publicAccess|hosting|files|commands' box-recon-results/box-v1.yaml | head -n 1600 || true
          else
            echo 'openapi unavailable'
          fi

          echo '=== official install script ==='
          curl -fsSL --max-time 20 -D box-recon-results/install.headers https://ascii.dev/api/box/install -o box-recon-results/install.sh || true
          if [ -s box-recon-results/install.sh ]; then
            echo "install_bytes=$(wc -c < box-recon-results/install.sh)"
            sed -n '1,260p' box-recon-results/install.sh
          else
            echo 'install script unavailable'
          fi

          echo '=== unauthenticated boundary probes ==='
          probe() {
            url="$1"
            echo "--- $url"
            rm -f /tmp/headers.tmp /tmp/body.tmp
            curl -sS --max-time 15 -o /tmp/body.tmp -D /tmp/headers.tmp -w 'HTTP=%{http_code} REDIRECT=%{redirect_url}\n' "$url" || true
            sed -n '1,80p' /tmp/headers.tmp 2>/dev/null || true
            echo '[body prefix]'
            head -c 1800 /tmp/body.tmp 2>/dev/null || true
            echo
          }
          probe https://ascii.dev/api/box/v1/boxes
          probe https://ascii.dev/api/box/v1/boxes/bx_sb5sbxzz
          probe https://ascii.dev/api/box/v1/snapshots
          probe https://ascii.dev/api/box/v1/environments

          echo '=== preflight/CORS ==='
          for path in boxes boxes/bx_sb5sbxzz snapshots environments; do
            echo "--- OPTIONS /$path"
            curl -sS --max-time 15 -o /dev/null -D - -X OPTIONS \
              -H 'Origin: https://attacker.invalid' \
              -H 'Access-Control-Request-Method: GET' \
              -H 'Access-Control-Request-Headers: authorization,content-type' \
              "https://ascii.dev/api/box/v1/$path" || true
          done

          echo '=== CLI ==='
          if [ -s box-recon-results/install.sh ]; then
            bash box-recon-results/install.sh || true
            export PATH="$HOME/.local/bin:$HOME/bin:/usr/local/bin:$PATH"
            command -v box || true
            box --version || true
            box --help || true
            for c in new get list fork snapshot desktop env ssh scp stop resume host; do
              echo "--- box $c --help"
              box "$c" --help || true
            done
          fi

      - name: Persist recon result
        shell: bash
        run: |
          git config user.name 'box-recon-bot'
          git config user.email 'box-recon-bot@users.noreply.github.com'
          git add -f box-recon-results
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m 'Record Box public recon output [skip ci]'
          git push origin HEAD:box-recon-20260821
